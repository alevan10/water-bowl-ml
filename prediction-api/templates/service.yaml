apiVersion: v1
kind: Service
metadata:
  name: {{ include "prediction-api.fullname" . }}
  labels:
    {{- include "prediction-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prediction-api.selectorLabels" . | nindent 4 }}
